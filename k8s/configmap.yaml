apiVersion: v1
kind: ConfigMap
metadata:
  name: ajj-config
  namespace: ajj-jujitsu
  labels:
    app: ajj-app
data:
  NODE_ENV: "production"
  PORT: "3000"
  DB_PATH: "/app/data/admin.db"
  JWT_EXPIRES_IN: "24h"
  DEFAULT_ADMIN_USERNAME: "admin"

# Made with Bob
